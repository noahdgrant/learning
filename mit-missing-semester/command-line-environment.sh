# jobs lists current jobs
# & at the end of a command runs the command in the background
# bg %X lets you interact with background jobs
# fg brings a background job to the foreground
# kill interacts with jobs and processes
# nohup can be used to ignore signals sent to the job


# you can append commands to the ssh command to get output in your terminal
# from the other machine

# scp can copy files over ssh
# scp notes.md noah@ip:notes.md

# ssh-copy-id can be used to copy key to remote machine
# rsync can be used for lots of files

# can setup .ssh/config with profiles to quickly ssh into a machine by specifying
# the details and giving it a nickname
